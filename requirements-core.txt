# =============================================================================
# PharmaDev AI — Layer 1: Core Runtime
# =============================================================================
# Minimal install.  Runs the headless analysis pipeline, bulk CSV processing,
# report generation (DOCX/PDF), and the SelfTest validation suite.
#
# What works at this layer:
#   - PropertyMapper, DevelopabilityCore (heuristic scoring), BispecificEngine
#   - Bulk analysis (bulk_schema → bulk_runner → bulk_summary)
#   - ReportAssembler → DOCX / PDF report generation
#   - CADET chromatography simulation (if engine binary present)
#   - All 18 SelfTest sections (289 substantive checks)
#   - pytest -m core (337 unit tests)
#
# What does NOT work:
#   - Streamlit UI              → install requirements-analysis.txt
#   - ESM-2 / XGBoost / torch ML models → install requirements-training.txt
#   - 3D protein visualization  → install requirements-analysis.txt
#
# Install: pip install -r requirements-core.txt
# =============================================================================

# -- Numerical & Data --------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0

# -- Bioinformatics ----------------------------------------------------------
biopython>=1.81

# -- Plotting (report figures — used by CharacterizationAgent, ReportAssembler)
plotly>=5.15.0
matplotlib>=3.7.0
kaleido>=0.2.1                # Plotly static image export (PNG/PDF)

# -- Document Generation (DOCX/PDF reports) -----------------------------------
python-docx>=0.8.11

# -- HDF5 / Chromatography Simulation (CADET Engine) -------------------------
h5py>=3.0.0

# -- Classical ML Baselines ---------------------------------------------------
# Required by src/baseline_runner.py and the public benchmark smoke tests.
scikit-learn>=1.3.0

# -- HTTP / Utilities ---------------------------------------------------------
requests>=2.31.0

# -- Testing (always available) -----------------------------------------------
pytest>=7.4.0
pytest-timeout>=2.3.1
